class Color:

    def __init__(self, red, blue, green):
        self.red = red
        self.blue = blue
        self.green = green

    def toBytes(self):
        return chr(self.red) + chr(self.blue) + chr(self.green)
